# gem(bindings/ruby)のCargo workspace。
#
# rb_sysの`ExtensionTask`はgemのルートで`cargo metadata`を実行してクレートを
# 探すため、ここにworkspaceを置く必要がある。リポジトリルートのworkspaceからは
# `exclude`されている(rb-sysのビルドがlibrubyを要求するため。
# docs/decisions/0062-ruby-binding.md 決定5)。
[workspace]
resolver = "2"
members = ["ext/sghtmltopdf"]
