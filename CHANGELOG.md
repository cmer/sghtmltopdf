# CHANGELOG

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## Unreleased

### Security

- HTMLのネストが深すぎる入力でスタックオーバーフローし、プロセスごと落ちる問題を修正。
  深さの上限(256)を超える入力はエラーで拒否するようになった。
- ヘッダに巨大な寸法だけを書いた小さなPNG/WebP(展開爆弾)で、確保に失敗してプロセスが落ちる問題を修正。
  展開後128MiBを超える画像は拒否するようになった。
- SOFセグメントの途中で切れたJPEGで境界外アクセスによりパニックする問題を修正。
  HTTPサーバモードではワーカースレッドが停止したままになっていた。
- HTTPサーバモードで、オプションの値に別のフラグを書くとリクエストから指定できないはずのオプション(`--allow-remote-assets`等)を有効化できた問題を修正。
- `@font-face`の`url()`が`--disable-local-file-access`と`--allow`を迂回してファイルを読めた問題を修正。
- リモート取得のブロック範囲を広げた。
  CGNAT(`100.64.0.0/10`)・`0.0.0.0/8`・`192.0.0.0/24`・`198.18.0.0/15`・`240.0.0.0/4`と、IPv6のTeredo・ドキュメント用・ORCHIDv2・discard-onlyが素通りしていた。
  IPv4-compatible(`::a.b.c.d`)・NAT64(`64:ff9b::/96`)・6to4(`2002::/16`)も、埋め込まれたIPv4側で判定するようにした(以前はIPv4-mappedしか見ておらず、IPv4側のフィルタを迂回できた)。
- HTTPサーバモードのクエリパラメータを拒否リストから許可リストへ変更。
  パスを取る新しいオプションを足したときに、分類し忘れがそのまま穴になることが無くなった。
- `--allow`の範囲判定を厳格にした。
  参照先を実パスへ解決できない場合、これまでは`..`を含んだままのパスで比較していた(`/var/www/../etc/passwd`が`/var/www`配下と判定されうる)。
  解決できない参照は拒否するようにし、許可ディレクトリの解決も起動時の1回だけにした。

### Added

- 保持するノード数の上限(50万)を追加。
  1リクエストのメモリ使用量が青天井だった問題への対処で、実測では10MiBの入力で1.8GiBに達していた。
  超えた場合はエラーで拒否する(サーバでは400)。
  ストリーミングモードでは処理済みのノードが数から外れるため、総量が上限を超えていても変換できる。
- HTTPサーバモードの`--timeout`が変換自体にも効くようになった。
  これまではキュー待ち時間にしか効かず、走り始めた変換は打ち切れなかったため、重いリクエストがワーカーを占有し続けていた。
  超過した場合は504を返す。
- CIに`cargo audit --deny warnings`を追加。
  判断済みのアドバイザリは`.cargo/audit.toml`にIDを明記して除外しているので、新しいものが増えたときだけ落ちる。
- Ruby gemに`Sghtmltopdf::InternalError`を追加。
  ネイティブ拡張の中でRustがパニックした場合、これまではRubyの`fatal`になり`rescue Exception`でも捕まえられずワーカープロセスごと終了していた。
  通常の`rescue`で受けられる例外に変換するようにした。

### Changed

- HTTPサーバモードの`--max-body-size`の既定を10MiBから4MiBへ変更(破壊的変更)。
  テキスト量に比例するメモリはノード数上限では抑えられず、実測で入力1MiBあたり約185MiBを使うため。
  大きな入力を扱う場合は明示的に引き上げること。
- `--allow`に実在しないディレクトリを指定するとエラーで終了するようになった(破壊的変更)。
  これまでは黙って受け入れていたが、許可範囲の判定が意図せず変わるため起動時に止める。
- ローカル参照が基準ディレクトリ(`--base-url`)の外へ出ることを既定で許さなくなった(破壊的変更)。
  `../`で外のファイルを参照していた場合は`--allow`で範囲を明示する必要がある。
  基準ディレクトリの中で完結する`../`はこれまでどおり使える。

## 0.1.0

### Added

- 1st release on 2026-08-02
